{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window talks to the threads through commands and listens for what they say. Nothing else is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
