{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window talks to the threads through commands, listens for what they say, and may put a finished errand on screen when nobody is looking at it. Nothing else is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
